// TODO
export function validateName(name) {
  return name.length > 0;
}

// TODO
export function validateEmail(email) {
  return email.length > 0;
}

// TODO
export function validateCreditCard(creditCart) {
  return creditCart.length > 0;
}
